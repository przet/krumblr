require 'rails_helper'

RSpec.describe Post, type: :model do

  #Delete all Blogs and Posts in ActiveRecord
  #Post.delete_all
  #Blog.delete_all

  #Create a Blog, of which posts will be part of
  #@blog = Blog.create({title:'Blog Post for Post tests',
  #                     id:'1'})

  #it "has 0 posts at the start" do
  #    expect(Post.count).to eq(0)
  #end

  #it "has 1 post after creating" do
  #        @post = Post.create({title: 'post1',
  #                             body: 'Hello'})
  #    expect(Post.count).to eq(1)
  #    @post.destroy
  #end
end
